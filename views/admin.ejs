<!DOCTYPE html>
<html>
<head>
    <title>Quản Trị Bot v12.0</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f4f6f9; margin: 0; padding: 20px; }
        .container { max-width: 1200px; margin: 0 auto; }
        .card { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); margin-bottom: 20px; }
        h2, h3 { margin-top: 0; color: #333; }
        label { font-weight: bold; display: block; margin-bottom: 5px; color: #555; }
        
        /* Form Luật */
        textarea.rules-box { width: 100%; height: 300px; padding: 10px; border: 1px solid #ddd; border-radius: 4px; font-family: monospace; font-size: 14px; box-sizing: border-box; }
        
        /* Bảng Sản Phẩm */
        .product-item { border: 1px solid #eee; padding: 15px; margin-bottom: 10px; border-radius: 6px; background: #fff; display: flex; gap: 15px; align-items: flex-start; }
        .product-img { width: 80px; height: 80px; object-fit: cover; border-radius: 4px; border: 1px solid #ddd; }
        .product-info { flex: 1; }
        .product-actions { display: flex; gap: 5px; }
        .badge { background: #e9ecef; padding: 2px 6px; border-radius: 4px; font-size: 12px; }
        
        /* Nút bấm */
        button { cursor: pointer; padding: 8px 15px; border-radius: 4px; border: none; font-weight: bold; }
        .btn-primary { background: #007bff; color: white; }
        .btn-danger { background: #dc3545; color: white; }
        .btn-success { background: #28a745; color: white; }
        .btn-warning { background: #ffc107; color: #333; }
        
        /* Modal Thêm SP */
        .input-group { margin-bottom: 10px; }
        input[type="text"], input[type="number"] { width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px; box-sizing: border-box; }
        
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        .logout { color: #dc3545; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>🤖 HỆ THỐNG QUẢN TRỊ BOT</h2>
            <a href="/logout" class="logout"><i class="fas fa-sign-out-alt"></i> Đăng Xuất</a>
        </div>

        <div class="card">
            <form method="POST" action="/admin/save-rules">
                <h3>📜 1. Luật Chung & Quy Trình (Core Rules)</h3>
                <p style="font-size: 13px; color: #666;">Chứa quy tắc ứng xử, chốt đơn, ship, vision... (Không chứa thông tin sản phẩm)</p>
                <textarea name="generalRules" class="rules-box"><%= generalRules %></textarea>
                <div style="margin-top: 10px; text-align: right;">
                    <button type="submit" class="btn-primary">💾 Lưu Luật</button>
                </div>
            </form>
        </div>

        <div class="card">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:15px;">
                <h3>📦 2. Danh Sách Sản Phẩm</h3>
                <button onclick="document.getElementById('addProductForm').style.display='block'" class="btn-success">+ Thêm Mới</button>
            </div>

            <div id="addProductForm" style="display:none; background: #f8f9fa; padding: 15px; border-radius: 6px; margin-bottom: 20px; border: 1px solid #ddd;">
                <form method="POST" action="/admin/save-product">
                    <input type="hidden" name="id" id="prodId">
                    <div class="input-group">
                        <label>Tên Sản Phẩm:</label>
                        <input type="text" name="name" id="prodName" required placeholder="VD: An Cung Samsung">
                    </div>
                    <div class="input-group" style="display:flex; gap:10px;">
                        <div style="flex:1">
                            <label>Giá Bán (VNĐ):</label>
                            <input type="text" name="price" id="prodPrice" required placeholder="VD: 780k (Freeship)">
                        </div>
                        <div style="flex:1">
                            <label>Link Ảnh:</label>
                            <input type="text" name="image" id="prodImage" placeholder="https://...">
                        </div>
                    </div>
                    <div class="input-group">
                        <label>Quà Tặng & Chính Sách:</label>
                        <input type="text" name="gift" id="prodGift" required placeholder="VD: Tặng 1 Dầu Lạnh. Mua 2 tặng 2.">
                    </div>
                    <div class="input-group">
                        <label>Mô Tả Chi Tiết (Thành phần, Date, Công dụng):</label>
                        <input type="text" name="desc" id="prodDesc" placeholder="VD: Date 10/2027. 15% Trầm Hương...">
                    </div>
                    <div style="text-align: right; gap: 5px; display: flex; justify-content: flex-end;">
                        <button type="button" onclick="resetForm()" style="background:#ccc;">Hủy</button>
                        <button type="submit" class="btn-success">Lưu Sản Phẩm</button>
                    </div>
                </form>
            </div>

            <div id="productList">
                <% products.forEach(function(p) { %>
                    <div class="product-item">
                        <img src="<%= p.image || 'https://via.placeholder.com/80' %>" class="product-img" onerror="this.src='https://via.placeholder.com/80'">
                        <div class="product-info">
                            <h4 style="margin: 0 0 5px 0;"><%= p.name %></h4>
                            <div style="color: #d63384; font-weight: bold;"><%= p.price %></div>
                            <div style="font-size: 13px; color: #28a745; margin-top: 3px;">🎁 <%= p.gift %></div>
                            <div style="font-size: 12px; color: #666; margin-top: 3px;">ℹ️ <%= p.desc %></div>
                        </div>
                        <div class="product-actions">
                            <button class="btn-warning" onclick="editProduct('<%= JSON.stringify(p).replace(/'/g, '&#39;') %>')"><i class="fas fa-edit"></i></button>
                            <form method="POST" action="/admin/delete-product" onsubmit="return confirm('Xóa sản phẩm này?')">
                                <input type="hidden" name="id" value="<%= p.id %>">
                                <button type="submit" class="btn-danger"><i class="fas fa-trash"></i></button>
                            </form>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>

    <script>
        function editProduct(jsonStr) {
            const p = JSON.parse(jsonStr);
            document.getElementById('prodId').value = p.id;
            document.getElementById('prodName').value = p.name;
            document.getElementById('prodPrice').value = p.price;
            document.getElementById('prodImage').value = p.image;
            document.getElementById('prodGift').value = p.gift;
            document.getElementById('prodDesc').value = p.desc;
            document.getElementById('addProductForm').style.display = 'block';
            window.scrollTo(0, document.getElementById('addProductForm').offsetTop);
        }
        function resetForm() {
            document.getElementById('addProductForm').style.display = 'none';
            document.getElementById('prodId').value = '';
            document.querySelector('#addProductForm form').reset();
        }
    </script>
</body>
</html>