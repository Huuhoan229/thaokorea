<!DOCTYPE html>
<html>
<head>
    <title>Quản Trị Bot v13.0</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f4f6f9; margin: 0; padding: 20px; }
        .container { max-width: 1200px; margin: 0 auto; }
        .card { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); margin-bottom: 20px; }
        h2, h3 { margin-top: 0; color: #333; }
        label { font-weight: bold; display: block; margin-bottom: 5px; color: #555; }
        
        /* Status Bar */
        .status-bar { display: flex; justify-content: space-between; align-items: center; background: #fff; padding: 15px 20px; border-radius: 8px; margin-bottom: 20px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }
        .status-indicator { display: flex; align-items: center; gap: 10px; font-weight: bold; font-size: 18px; }
        .dot { height: 15px; width: 15px; border-radius: 50%; display: inline-block; }
        .active { color: #28a745; } .active .dot { background-color: #28a745; }
        .inactive { color: #dc3545; } .inactive .dot { background-color: #dc3545; }

        /* Form Luật */
        textarea.rules-box { width: 100%; height: 200px; padding: 10px; border: 1px solid #ddd; border-radius: 4px; font-family: monospace; }
        
        /* Bảng & Input */
        .item-list { border-top: 1px solid #eee; margin-top: 15px; }
        .item-row { padding: 15px 0; border-bottom: 1px solid #eee; display: flex; justify-content: space-between; align-items: center; }
        input[type="text"], input[type="number"] { width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px; box-sizing: border-box; margin-bottom: 10px; }
        
        /* Nút bấm */
        button { cursor: pointer; padding: 8px 15px; border-radius: 4px; border: none; font-weight: bold; color: white; }
        .btn-primary { background: #007bff; }
        .btn-danger { background: #dc3545; }
        .btn-success { background: #28a745; }
        .btn-warning { background: #ffc107; color: #333; }
        .btn-toggle { padding: 10px 20px; font-size: 16px; }
        
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        .logout { color: #dc3545; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>🤖 HỆ THỐNG QUẢN TRỊ BOT</h2>
            <a href="/logout" class="logout"><i class="fas fa-sign-out-alt"></i> Đăng Xuất</a>
        </div>

        <div class="status-bar">
            <div class="status-indicator <%= systemStatus ? 'active' : 'inactive' %>">
                <span class="dot"></span>
                <span><%= systemStatus ? 'HỆ THỐNG ĐANG BẬT' : 'HỆ THỐNG ĐANG TẠM DỪNG' %></span>
            </div>
            <form method="POST" action="/admin/toggle-system">
                <input type="hidden" name="status" value="<%= !systemStatus %>">
                <% if(systemStatus) { %>
                    <button type="submit" class="btn-danger btn-toggle"><i class="fas fa-power-off"></i> TẠM DỪNG BOT</button>
                <% } else { %>
                    <button type="submit" class="btn-success btn-toggle"><i class="fas fa-play"></i> BẬT BOT</button>
                <% } %>
            </form>
        </div>

        <div class="card">
            <h3>🌐 1. Quản Lý Fanpage Kết Nối</h3>
            <p style="font-size: 13px; color: #666;">Thêm Page ID và Token để Bot chạy trên nhiều Fanpage.</p>
            
            <button onclick="document.getElementById('addPageForm').style.display='block'" class="btn-success">+ Thêm Page Mới</button>
            
            <div id="addPageForm" style="display:none; background: #f8f9fa; padding: 15px; margin-top: 15px; border: 1px solid #ddd;">
                <form method="POST" action="/admin/save-page">
                    <label>Tên Page (Gợi nhớ):</label>
                    <input type="text" name="name" required placeholder="VD: Thảo Korea Chính">
                    <label>Page ID:</label>
                    <input type="text" name="pageId" required placeholder="VD: 1000xxxxxxx">
                    <label>Access Token:</label>
                    <input type="text" name="token" required placeholder="VD: EAA...">
                    <div style="text-align: right;">
                        <button type="button" onclick="document.getElementById('addPageForm').style.display='none'" style="background:#ccc; color:#333">Hủy</button>
                        <button type="submit" class="btn-primary">Lưu Page</button>
                    </div>
                </form>
            </div>

            <div class="item-list">
                <% pages.forEach(function(p) { %>
                    <div class="item-row">
                        <div>
                            <strong><%= p.name %></strong> <br>
                            <small style="color:#666">ID: <%= p.pageId %></small>
                        </div>
                        <form method="POST" action="/admin/delete-page" onsubmit="return confirm('Xóa Page này? Bot sẽ ngừng chạy trên Page đó.')">
                            <input type="hidden" name="id" value="<%= p.id %>">
                            <button type="submit" class="btn-danger"><i class="fas fa-trash"></i></button>
                        </form>
                    </div>
                <% }); %>
            </div>
        </div>

        <div class="card">
            <form method="POST" action="/admin/save-rules">
                <h3>📜 2. Luật Chung & Quy Trình</h3>
                <textarea name="generalRules" class="rules-box"><%= generalRules %></textarea>
                <div style="margin-top: 10px; text-align: right;">
                    <button type="submit" class="btn-primary">💾 Lưu Luật</button>
                </div>
            </form>
        </div>

        <div class="card">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:15px;">
                <h3>📦 3. Danh Sách Sản Phẩm</h3>
                <button onclick="document.getElementById('addProductForm').style.display='block'" class="btn-success">+ Thêm SP Mới</button>
            </div>

            <div id="addProductForm" style="display:none; background: #f8f9fa; padding: 15px; margin-bottom: 20px; border: 1px solid #ddd;">
                <form method="POST" action="/admin/save-product">
                    <input type="hidden" name="id" id="prodId">
                    <label>Tên Sản Phẩm:</label>
                    <input type="text" name="name" id="prodName" required>
                    <div style="display:flex; gap:10px;">
                        <div style="flex:1"><label>Giá:</label><input type="text" name="price" id="prodPrice" required></div>
                        <div style="flex:1"><label>Ảnh:</label><input type="text" name="image" id="prodImage"></div>
                    </div>
                    <label>Quà Tặng:</label><input type="text" name="gift" id="prodGift" required>
                    <label>Mô Tả:</label><input type="text" name="desc" id="prodDesc">
                    <div style="text-align: right;">
                        <button type="button" onclick="resetForm()" style="background:#ccc; color:#333">Hủy</button>
                        <button type="submit" class="btn-success">Lưu SP</button>
                    </div>
                </form>
            </div>

            <% products.forEach(function(p) { %>
                <div class="item-row">
                    <div style="display:flex; gap:10px; align-items:center;">
                        <img src="<%= p.image %>" style="width:50px; height:50px; object-fit:cover; border-radius:4px;" onerror="this.src='https://via.placeholder.com/50'">
                        <div>
                            <strong><%= p.name %></strong> - <span style="color:#d63384"><%= p.price %></span><br>
                            <small style="color:#28a745">🎁 <%= p.gift %></small>
                        </div>
                    </div>
                    <div style="display:flex; gap:5px;">
                        <button class="btn-warning" onclick="editProduct('<%= JSON.stringify(p).replace(/'/g, '&#39;').replace(/"/g, '&quot;') %>')"><i class="fas fa-edit"></i></button>
                        <form method="POST" action="/admin/delete-product" onsubmit="return confirm('Xóa SP này?')">
                            <input type="hidden" name="id" value="<%= p.id %>">
                            <button type="submit" class="btn-danger"><i class="fas fa-trash"></i></button>
                        </form>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <script>
        function editProduct(jsonStr) {
            const p = JSON.parse(jsonStr);
            document.getElementById('prodId').value = p.id;
            document.getElementById('prodName').value = p.name;
            document.getElementById('prodPrice').value = p.price;
            document.getElementById('prodImage').value = p.image;
            document.getElementById('prodGift').value = p.gift;
            document.getElementById('prodDesc').value = p.desc;
            document.getElementById('addProductForm').style.display = 'block';
            window.scrollTo(0, document.getElementById('addProductForm').offsetTop);
        }
        function resetForm() {
            document.getElementById('addProductForm').style.display = 'none';
            document.getElementById('prodId').value = '';
            document.querySelector('#addProductForm form').reset();
        }
    </script>
</body>
</html>