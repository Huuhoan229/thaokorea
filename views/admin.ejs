<!DOCTYPE html>
<html>
<head>
    <title>Admin Shop Thảo Korea</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; padding: 20px; }
        .card { margin-bottom: 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .status-badge { font-size: 0.8em; padding: 5px 10px; border-radius: 10px; }
        .bg-instock { background-color: #d4edda; color: #155724; }
        .bg-outstock { background-color: #f8d7da; color: #721c24; }
    </style>
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="text-primary fw-bold">⚙️ ADMIN SHOP THẢO KOREA</h2>
            <a href="/logout" class="btn btn-outline-danger">Đăng xuất</a>
        </div>

        <div class="card border-warning">
            <div class="card-header bg-warning text-dark fw-bold">🎁 TRẠNG THÁI QUÀ TẶNG (Hết hàng thì tắt đi)</div>
            <div class="card-body">
                <form action="/admin/save-gifts" method="POST" class="row g-3 align-items-center">
                    <div class="col-auto">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" name="dauLanh" value="true" <%= giftConfig.dauLanh ? 'checked' : '' %>>
                            <label class="form-check-label fw-bold">Dầu Lạnh</label>
                        </div>
                    </div>
                    <div class="col-auto">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" name="caoDan" value="true" <%= giftConfig.caoDan ? 'checked' : '' %>>
                            <label class="form-check-label fw-bold">Cao Dán</label>
                        </div>
                    </div>
                    <div class="col-auto">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" name="keoSam" value="true" <%= giftConfig.keoSam ? 'checked' : '' %>>
                            <label class="form-check-label fw-bold">Kẹo Sâm</label>
                        </div>
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-sm btn-primary">Lưu Trạng Thái Quà</button>
                    </div>
                </form>
            </div>
        </div>

        <div class="card">
            <div class="card-header bg-primary text-white fw-bold">Cài đặt AI & Hệ thống</div>
            <div class="card-body">
                <form action="/admin/save-ai" method="POST" class="row g-2 mb-3">
                    <div class="col-md-8"><input type="text" name="apiKey" class="form-control" placeholder="Gemini API Key" value="<%= aiConfig.apiKey %>"></div>
                    <div class="col-md-4"><button type="submit" class="btn btn-success w-100">Lưu API</button></div>
                </form>
                <form action="/admin/toggle-system" method="POST">
                    <div class="form-check form-switch">
                        <input class="form-check-input" type="checkbox" onchange="this.form.submit()" name="status" value="true" <%= systemStatus ? 'checked' : '' %>>
                        <label class="form-check-label fw-bold"><%= systemStatus ? 'Hệ thống ĐANG BẬT' : 'Hệ thống ĐANG TẮT' %></label>
                    </div>
                </form>
            </div>
        </div>

        <div class="card">
            <div class="card-header bg-info text-white fw-bold">Danh Sách Sản Phẩm (Hết hàng thì chọn Tắt)</div>
            <div class="card-body">
                <table class="table table-hover align-middle">
                    <thead><tr><th>Tên SP</th><th>Giá</th><th>Kho</th><th>Hành động</th></tr></thead>
                    <tbody>
                        <% products.forEach(p => { %>
                        <form action="/admin/save-product" method="POST">
                            <input type="hidden" name="id" value="<%= p.id %>">
                            <input type="hidden" name="name" value="<%= p.name %>">
                            <input type="hidden" name="price" value="<%= p.price %>">
                            <input type="hidden" name="gift" value="<%= p.gift %>">
                            <input type="hidden" name="desc" value="<%= p.desc %>">
                            <input type="hidden" name="image" value="<%= p.image %>">
                            <tr>
                                <td><%= p.name %></td>
                                <td><%= p.price %></td>
                                <td>
                                    <select name="inStock" class="form-select form-select-sm <%= p.inStock ? 'bg-instock' : 'bg-outstock' %>" onchange="this.form.submit()">
                                        <option value="true" <%= p.inStock ? 'selected' : '' %>>Còn hàng</option>
                                        <option value="false" <%= !p.inStock ? 'selected' : '' %>>HẾT HÀNG</option>
                                    </select>
                                </td>
                                <td>
                                    <button type="button" class="btn btn-sm btn-warning" data-bs-toggle="modal" data-bs-target="#editModal<%= p.id %>">Sửa</button>
                                    <button type="submit" formaction="/admin/delete-product" class="btn btn-sm btn-danger">Xóa</button>
                                </td>
                            </tr>
                        </form>

                        <div class="modal fade" id="editModal<%= p.id %>" tabindex="-1">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <form action="/admin/save-product" method="POST">
                                        <div class="modal-header"><h5 class="modal-title">Sửa SP</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div>
                                        <div class="modal-body">
                                            <input type="hidden" name="id" value="<%= p.id %>">
                                            <div class="mb-2"><label>Tên SP</label><input type="text" name="name" class="form-control" value="<%= p.name %>"></div>
                                            <div class="mb-2"><label>Giá</label><input type="text" name="price" class="form-control" value="<%= p.price %>"></div>
                                            <div class="mb-2"><label>Link Ảnh</label><input type="text" name="image" class="form-control" value="<%= p.image %>"></div>
                                            <div class="mb-2"><label>Mô tả/Link Video</label><textarea name="desc" class="form-control"><%= p.desc %></textarea></div>
                                            <div class="mb-2"><label>Trạng thái</label>
                                                <select name="inStock" class="form-control">
                                                    <option value="true" <%= p.inStock ? 'selected' : '' %>>Còn hàng</option>
                                                    <option value="false" <%= !p.inStock ? 'selected' : '' %>>Hết hàng</option>
                                                </select>
                                            </div>
                                        </div>
                                        <div class="modal-footer"><button type="submit" class="btn btn-primary">Lưu</button></div>
                                    </form>
                                </div>
                            </div>
                        </div>
                        <% }) %>
                    </tbody>
                </table>
                
                <h5 class="mt-4">Thêm Sản Phẩm Mới</h5>
                <form action="/admin/save-product" method="POST" class="row g-2">
                    <input type="hidden" name="inStock" value="true">
                    <div class="col-md-3"><input type="text" name="name" class="form-control" placeholder="Tên SP" required></div>
                    <div class="col-md-2"><input type="text" name="price" class="form-control" placeholder="Giá" required></div>
                    <div class="col-md-3"><input type="text" name="image" class="form-control" placeholder="Link Ảnh"></div>
                    <div class="col-md-3"><input type="text" name="desc" class="form-control" placeholder="Mô tả/Video"></div>
                    <div class="col-md-1"><button type="submit" class="btn btn-success w-100">Thêm</button></div>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>